---
_defaults:
  command:
    install
  deps:
    apt:
      - virtualenv
  install_requirements:
    no
  python_exe:
    python3
  requirements_file:
    requirements.txt
  site_packages:
    no

setuptools:
  {}
